# Widgets (reusable View pieces)

Small, reusable UI building blocks used by the screens in `views/`.

Examples for the Todo app: a single task row, the "add task" input box, and
any buttons or list items we want to keep separate from the main screen so
each file stays small and easy to read.
